version: '3'

services:
  php:
    build:
      context: .
    volumes:
      - ./:/var/www/html
    environment:
      APP_DEBUG: "true"

  web:
    image: nginx:1.17
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html       
      - .docker/nginx/nginx_template_prod.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"

  db:
    image: mysql:8
    environment:
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_ROOT_HOST: "%"
      MYSQL_ROOT_PASSWORD: "${DB_PASSWORD}"
      MYSQL_DATABASE: "${DB_DATABASE}"
      MYSQL_USER: "${DB_USERNAME}"
      MYSQL_PASSWORD: "${DB_PASSWORD}"
    volumes:
      - mysqldata:/var/lib/mysql

  redis:
    image: redis:5
    volumes:
      - redisdata:/data

volumes:
  mysqldata:
  redisdata:
